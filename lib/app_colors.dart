import 'package:flutter/material.dart';

/// Centralized color palette for EduGram admin screens.
/// Import this file instead of repeating color constants in each screen.
const appBlue = Color(0xFF2196F3);
const appBlueDark = Color(0xFF1565C0);
const appBlueSoft = Color(0xFFE8F1FD);
const appDark = Color(0xFF252D3D);
const appDarkGrad = Color(0xFF374357);
const appBg = Colors.white;
const appDarkText = Color(0xFF1C2233);
const appSubText = Color(0xFF8C96A8);
const appCardBg = Colors.white;
const appDivider = Color(0xFFDCE2EF); // Slightly darker for contrast against white
const appInputBg = Color(0xFFF6F7FB);
